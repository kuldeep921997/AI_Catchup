// Crisil interview prep — a focused, JD-specific sprint built after the
// recruiter call for the "Full Stack Engineer (Python)" / Python Backend
// Engineer, Trading team role.
//
// Same block schema as the other two tracks (see ../blocks.js). Ordering
// follows the JD itself: Python/async fundamentals and the named frameworks
// first (mandatory skills), then the infrastructure pieces (WebSocket, SQL,
// Redis, microservices), then the trading-domain modules — which lean
// directly on real Algonauts experience rather than starting from zero —
// and finally the interview-narrative module for the frontend/backend gap
// the recruiter's own feedback made explicit.

import c01 from "./c01";
import c02 from "./c02";
import c03 from "./c03";
import c04 from "./c04";
import c05 from "./c05";
import c06 from "./c06";
import c07 from "./c07";
import c08 from "./c08";
import c09 from "./c09";

const crisil = [c01, c02, c03, c04, c05, c06, c07, c08, c09];

export default crisil;
